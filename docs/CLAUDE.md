# CLAUDE.md — 프로젝트 자동 진입점

이 파일은 Claude Code가 세션 시작 시 자동 로드한다.
실제 프로젝트 지침과 문서 라우팅은 오케스트레이터 `core.md`에 있다.

## 먼저 읽어라
@core.md

---

## 🔴 절대 규칙 (YOU MUST — 어떤 작업에서도 위반 금지)

1. **YOU MUST: LLM API 키를 클라이언트 코드에 절대 노출하지 마라.**
   모든 LLM 호출은 서버(Route Handler / Functions)에서만 한다.
   `NEXT_PUBLIC_` 접두사에 LLM 키를 넣는 것은 금지다.

2. **YOU MUST: MVP 범위를 임의로 넓히지 마라.**
   엔진 모듈은 1개만 구현한다(`docs/05-roadmap.md` 참조).
   "음악·PPT·카드뉴스 다 만들기"는 2차 범위다. 요청받지 않은 모듈을 추가하지 마라.

3. **IMPORTANT: 결정되지 않은 항목(`[결정 필요]`)은 임의로 확정하지 말고,
   합리적 기본값으로 구현하되 코드 주석과 작업 요약에 `// DECISION:` 으로 명시하라.**

4. 새 의존성·외부 서비스를 추가하기 전에 `core.md`의 스택 정의와 충돌하는지 확인하라.

---

## 작업 시작 체크리스트
- [ ] `core.md`의 라우팅 표에서 이 작업에 해당하는 `docs/*` 를 읽었는가
- [ ] 위 절대 규칙 4개를 위반하지 않는가
- [ ] 변경이 MVP Phase 범위 안인가 (`docs/05-roadmap.md`)
